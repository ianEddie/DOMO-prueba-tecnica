---
import animation from '@/animations/reveal.module.css'
---
<article class={animation.reveal}>
  <h2>Get in touch with us</h2>
  <p>Maecenas sed diam eget risus varius blandit sit amet non magna.</p>
  <button class="btn btn-default">Hire us</button>
</article>
<style>
  article {
    background-color: var(--bg-color-secondary);
    padding-inline: 20px;
    padding-block: 40px;
    display: flex;
    flex-direction: column;
    align-items: start;
    justify-content: center;
    row-gap: 10px;
  }
  h2 {
    color: var(--text-color-primary);
    font-size: 25px;
    font-weight: 500;
  }
  p {
    color: var(--text-color-primary);
    filter: opacity(80%);
    max-width: 60%;
    font-size: 13px;
    text-wrap: pretty;q
  }
  button{
    color: var(--text-color-primary);
    border-radius: 0;
    border-color: var(--text-color-primary);
    padding-inline: 20px;
  }
  @media screen and (max-width: 992px){
    article{
      width: 100%;
    }
  }
</style>
